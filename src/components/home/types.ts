export interface TestimonialType {
  name: string;
  company: string;
  content: string;
  image: string;
}

export interface FeatureType {
  title: string;
  description: string;
  icon: React.ElementType;
}